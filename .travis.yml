sudo: required 
language: node_js
node_js:
    - "stable"
    
services: 
    - docker
before_install: 
    - docker build -t gowithkk/sample-app -f Dockerfile.dev .
script:     
    - docker run -e CI=true gowithkk/sample-app npm run test -- --coverage --watchAll=false